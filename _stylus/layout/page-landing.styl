//
.landing
  .current-video
    column(2/3, $cycle:2)
  .sidebar
    column(1/3, $cycle:2)
    h1
      text-transform uppercase
      color $link-color
      font-size 24px
      text-align center
    img
      max-width 100%
  .other-videos
    span()
    uncycle(2)
